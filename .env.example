# Supabase — client (exposed to the browser via Vite)
VITE_SUPABASE_URL="https://<project-ref>.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="<anon-key>"

# Supabase — server (JWT verification in server functions)
SUPABASE_URL="https://<project-ref>.supabase.co"
SUPABASE_PUBLISHABLE_KEY="<anon-key>"
SUPABASE_SERVICE_ROLE_KEY="<service-role-key>"

# AI provider (any OpenAI-compatible chat-completions API)
AI_API_KEY="<api-key>"
AI_BASE_URL="https://api.openai.com/v1"
AI_MODEL="gpt-5"

# Cloudflare Workers AI — server-only, generates the outfit visual after
# Gemini creates the written look. Image generation degrades gracefully
# (written outfit still works) when these are unset.
CLOUDFLARE_ACCOUNT_ID="<cloudflare-account-id>"
CLOUDFLARE_API_TOKEN="<cloudflare-api-token>"
IMAGE_MODEL="@cf/black-forest-labs/flux-1-schnell"

VITE_HCAPTCHA_SITEKEY="<hcaptcha-sitekey>"

# Dev admin account (see README "Auth & routes" for bootstrap steps)
ADMIN_EMAIL="<admin-email>"
ADMIN_PASSWORD="<admin-password>"

# Dev moderator account
MODERATOR_EMAIL="<moderator-email>"
MODERATOR_PASSWORD="<moderator-password>"
