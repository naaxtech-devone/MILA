# Mila — End of Day Update, 2026-07-09

[DONE]

Dashboard
- added a new Look section component to the dashboard and simplified the dashboard route to use it
- cleaned up the Dashboard function/route code

New admin dashboard
- split the admin panel into proper sub-routes: overview, members, moderation, and support (was one large 425-line file before)
- moved the main app routes (dashboard, feed, history, style profile) under a shared "_app" layout
- built a new AdminShell layout and AdminSidebar component to replace the old app-shell/desktop-nav setup
- added an AdminStatCard component and admin data helpers/queries (admin.functions.ts, queries/admin.ts) to power the new admin pages
- built a new AdminHeader component, then iterated on it (removed unused user info shown in the header)
- added a working sidebar toggle (collapse/expand) with animations, and improved sign-out button styling in the sidebar

Sign-out flow
- added a reusable useSignOut hook so sign-out behaves the same across admin
- added a signingOut loading state to the Auth context and updated AuthLayout so navigation waits for sign-out to finish
- added the same signingOut loading state to the membership drawer's sign-out button

Navigation & login by role
- updated SiteHeader to show/hide nav links depending on whether a user is signed in
- updated SiteHeader, the login page, and the home route so navigation adapts based on the user's role (e.g. admin vs regular member)

Google sign-in
- implemented the Google OAuth redirect flow and added a new /auth/callback route to complete it

[TO DO]
- will analyze the UI/UX to make it more user friendly — how does it work, like adding empty states, skeleton loaders, etc.
