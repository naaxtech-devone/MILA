MILA — IN DEVELOPMENT FEATURES
Last updated: 2026-07-11

Purpose
-------
This file lists functionality intentionally disabled or marked as in
development for the current deployment. It was produced by a full-repository
audit against the actual code (not the README) — only confirmed unfinished
functionality is listed here. Completed functionality is not included.

Status definitions
------------------
PARTIAL      Some functionality works, but specific actions remain unavailable.
DEVELOPMENT  The feature is visible but not complete.
DISABLED     The feature is intentionally unavailable in this deployment.

======================================================================
FEATURE: credit-purchases
STATUS: DEVELOPMENT

User-facing area:
- "Studio Energy Depleted" credit-pack dialog (opened when a credit
  paywall would trigger, and from the header credit counter).

Routes:
- /dashboard (and any other authenticated page via the shared app shell)

Source files:
- src/components/dashboard/upgrade-slots-dialog.tsx
- src/constants/app.ts (CREDIT_PACKS data, unchanged — still used for preview)

Deployment behavior:
- The three credit-pack buttons are rendered with the native `disabled`
  attribute and reduced opacity — they no longer look or behave as
  clickable.
- An "In development" badge is shown in the dialog header.
- A DevelopmentNotice explains that purchasing isn't available yet and
  that existing daily credits still work.
- No toast-as-fake-success and no network/payment call is triggered by
  these buttons (previously they only showed a "billing comes online
  soon" toast — that fake-success-shaped interaction was removed).

Reason:
- No payment provider (Stripe or otherwise) is integrated anywhere in the
  repository. The `purchases` table exists in Postgres but has no client
  writer and no webhook — by design, its RLS policy only grants SELECT to
  authenticated users, so a purchase row can only ever be created by a
  trusted server process that does not yet exist.

Remaining work:
- Choose and configure a payment provider.
- Add server-side purchase verification (webhook-driven, not
  client-reported).
- Connect verified purchases to `user_entitlements` updates.
- Add failure and refund handling.

======================================================================
FEATURE: membership-passes
STATUS: DEVELOPMENT

User-facing area:
- Membership drawer ("Acquire Passes" and "View Partner Editorial"
  actions on the Concierge Access panel).

Routes:
- /dashboard, /feed, /style-profile, /history (anywhere the account/
  membership drawer is reachable via the app shell)

Source files:
- src/components/account/studio-membership-drawer.tsx

Deployment behavior:
- Both buttons are rendered with the native `disabled` attribute.
- An "In development" badge is shown on the primary button; a
  DevelopmentNotice explains the partner-editorial mechanic is not yet
  available.
- The now-unused `onAcquirePasses` / `onWatchEditorial` optional callback
  props were removed from the component's public interface — no caller in
  the repo ever passed them, so clicking these buttons previously did
  nothing silently (a misleading "looks active, does nothing" state).
  They are now visibly inert instead.
- All other membership-drawer functionality (email change, password
  change, data export, sign out, links to Style Profile and History)
  remains fully working and was not touched.

Reason:
- No pass-acquisition or partner-editorial flow — nor any handler for
  either action — exists anywhere in the codebase.

Remaining work:
- Design and build the pass-acquisition flow (likely depends on
  credit-purchases above).
- Build or license the "partner editorial" content flow.
- Re-add typed callback props once real handlers exist.

======================================================================
FEATURE: credit-enforcement
STATUS: DEVELOPMENT

User-facing area:
- None directly — this is a backend behavior. The credit counter shown in
  the app header ("N AI credits") reads a real `user_entitlements.
  ai_credits` value, but that value never decreases because consumption
  is not yet wired up.

Routes:
- All authenticated routes (via the shared app shell credit counter)

Source files:
- src/lib/credits.server.ts (consumeAiCredit — always returns 999,
  never deducts)
- src/lib/credits.ts (isInsufficientCreditsError — always returns false,
  so the paywall branch that opens the credit-purchases dialog is
  currently unreachable)

Deployment behavior:
- AI-powered features (Generate Look, Lens outfit analysis, Concierge
  chat, Dupe Hunter) remain fully enabled and usable — per the
  deployment brief, credit enforcement being incomplete must not block
  otherwise-working AI functionality.
- The credit counter continues to display a real (not hardcoded) number
  from the database; it just does not yet reflect real usage.
- Source comments were added so this is not mistaken for an oversight.

Reason:
- The premium/credit gate was intentionally stubbed out in prior work
  ("Re-enable premium gate before production launch") pending a real
  purchase flow (see credit-purchases above) — enforcing a limit with no
  way to buy more credits would be a dead end for users.

Remaining work:
- Re-enable real credit deduction in consumeAiCredit.
- Re-enable the insufficient-credits check once credit-purchases ships.
- Add a credits ledger table if historical "credits issued" reporting is
  needed (the admin dashboard currently shows a running balance, not a
  historical total — see src/lib/admin.functions.ts:370-371).

======================================================================
FEATURE: ad-rewards
STATUS: DEVELOPMENT

User-facing area:
- None. No UI exists anywhere in the app for this feature.

Routes:
- None.

Source files:
- None modified — there is no client code referencing this feature to
  disable. The `ad_events` table exists in
  supabase/migrations/20260706102649_create_full_schema.sql with no
  INSERT policy (by design, per its own migration comment: "clients
  could otherwise mint [rewards]"), and no ad SDK, "Watch ad" button, or
  reward-granting server function exists anywhere in src/.

Deployment behavior:
- Nothing to disable; nothing is exposed. Listed here for completeness
  of the deployment inventory per the audit brief.

Reason:
- Feature was never built past the database schema.

Remaining work:
- Integrate an ad SDK.
- Build a trusted (server-verified) reward-granting callback — never a
  client-writable one.
- Add the corresponding INSERT policy scoped to that trusted path only.

======================================================================
FEATURE: concierge-chat
STATUS: DEVELOPMENT

User-facing area:
- "Concierge" entry in the desktop top nav and mobile tab bar (opens
  "Mila's Styling Studio" chat drawer).

Routes:
- All authenticated routes (nav is part of the shared app shell)

Source files:
- src/components/layout/desktop-nav.tsx
- src/components/layout/mobile-tab-bar.tsx
- src/components/layout/app-shell.tsx (removed the now-dead
  isConciergeOpen state and StylistConciergeDrawer render — nothing could
  open it anymore once both nav triggers were disabled)
- src/components/dashboard/stylist-concierge-drawer.tsx (documentation
  comment only — component left intact and unwired for future use)

Deployment behavior:
- The Concierge nav button is rendered with the native `disabled`
  attribute on desktop and mobile, with a compact "In development"
  indicator and an accessible description via `aria-describedby`/
  `aria-label`.
- The drawer can no longer be opened from anywhere in the app, so no AI
  chat request can be triggered in a broken state.

Reason:
- Found during manual testing (not caught by static audit): the chat
  only calls the real AI backend (`fixOutfitChat`) when it has an
  anchored outfit photo (`item.imageUrl`). `StylistConciergeDrawer` was
  rendered in exactly one place in the whole app
  (src/components/layout/app-shell.tsx), always with `item={null}`. With
  no `item`, `send()` never calls the AI at all — it always returns the
  same canned "Snap or upload an outfit through the Studio Lens" reply,
  regardless of what's asked. Since the UI's own quick-prompt suggestions
  (e.g. "Which neutrals anchor a [season] palette?") imply general
  styling Q&A works, this was actively misleading rather than merely
  incomplete.

Remaining work:
- Build a real path to anchor an item (e.g. open Concierge from a Lens
  result or a History item with its photo attached).
- Re-enable the nav entry once general (photo-less) questions either get
  a real answer or the UI stops implying they will.

======================================================================
FEATURE: moderator-role
STATUS: DEVELOPMENT

User-facing area:
- Admin → Members page (development notice shown above the members
  table).

Routes:
- /admin/members

Source files:
- src/routes/_authenticated/admin/members.tsx

Deployment behavior:
- A DevelopmentNotice is shown on the Members page clarifying that only
  the Steward (admin) role can be granted in this release.
- No UI change was needed to "disable" a moderator-assignment control,
  because none exists — the notice exists to set correct expectations,
  not to hide a broken control.
- All admin functionality that IS complete (Steward grant/revoke, member
  suspend/reinstate, member create/edit, moderation queue, support inbox)
  remains fully working and was not touched.

Reason:
- The `moderator` value exists in the Postgres `app_role` enum, but no
  application code reads, assigns, or authorizes against it — every
  admin-authorization check (`assertAdmin`, the `has_role` RPC call, the
  admin route guards) is hardcoded to check for `'admin'` only. It is a
  dead enum value today.

Remaining work:
- Design moderator-specific permissions (a defined subset of admin
  capabilities).
- Add a role-assignment control distinct from the Steward toggle.
- Add moderator-aware authorization checks alongside the existing
  admin-only checks, without weakening them.

======================================================================
FEATURE: password-reset
STATUS: DEVELOPMENT

User-facing area:
- None currently. The login screen has no "Forgot password?" link or
  action to disable.

Routes:
- /login

Source files:
- None modified — there is no existing reset-password UI, link, or route
  to disable. src/routes/login.tsx and its login-form component were
  read in full; no "Forgot password" control, no call to
  `supabase.auth.resetPasswordForEmail`, and no reset-confirmation route
  exist anywhere in the repository.

Deployment behavior:
- No code change was made. Per the deployment brief, this task does not
  add a reset-password implementation and only disables an existing
  reset action if one is exposed — none is.
- Listed here because a locked-out user currently has no self-service
  account-recovery path at all, which is directly relevant to release
  planning.

Reason:
- Feature was never built.

Remaining work:
- Add a "Forgot password?" link on the login screen.
- Implement `supabase.auth.resetPasswordForEmail` + a reset-confirmation
  route.
- Add rate limiting / captcha consistent with the existing signup flow's
  hCaptcha usage.

======================================================================

ALL FILES MODIFIED FOR DEVELOPMENT MARKERS
------------------------------------------
- src/components/dashboard/upgrade-slots-dialog.tsx
- src/components/account/studio-membership-drawer.tsx
- src/lib/credits.ts
- src/lib/credits.server.ts
- src/routes/_authenticated/admin/members.tsx
- src/components/layout/desktop-nav.tsx
- src/components/layout/mobile-tab-bar.tsx
- src/components/dashboard/stylist-concierge-drawer.tsx

FILES MODIFIED TO REMOVE NOW-DEAD WIRING (no marker themselves — the
disabled entry points they wired up were removed)
------------------------------------------------------------------------
- src/components/layout/app-shell.tsx

SUPPORTING INFRASTRUCTURE ADDED (no feature markers themselves — shared
plumbing the files above depend on)
------------------------------------------------------------------------
- src/config/features.ts             (central feature-availability registry)
- src/components/ui/development-badge.tsx    (reusable "In development" badge)
- src/components/ui/development-notice.tsx   (reusable inline notice)
- src/components/ui/development-page.tsx     (reusable full-route placeholder;
                                               not currently used by any route
                                               — every existing route was
                                               confirmed fully functional
                                               during this audit)
